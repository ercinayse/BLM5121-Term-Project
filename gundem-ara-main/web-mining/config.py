import os
from dotenv import load_dotenv

load_dotenv()

# Database configuration
DB_CONFIG = {
    'dbname': 'testdb',
    'user': 'postgres',
    'password': 'Ayse.868',
    'host': 'localhost',
    'port': '5432'
}